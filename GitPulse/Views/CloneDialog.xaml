<Window x:Class="GitPulse.Views.CloneDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clone Repository" Width="520" Height="235"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgBaseBrush}" Foreground="{StaticResource TextBrush}"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Repository URL" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
        <TextBox Grid.Row="1" x:Name="UrlBox"
                 Text="https://github.com/user/repo.git"/>

        <TextBlock Grid.Row="2" Text="Local Destination Path" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,14,0,6"/>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PathBox" Grid.Column="0"
                     VerticalAlignment="Top"/>
            <Button Grid.Column="1" Content="Browse..."
                    Click="Browse_Click" Margin="8,0,0,0"
                    Width="84" Height="32"
                    Style="{StaticResource SmallButtonStyle}"/>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Click="Cancel_Click" IsCancel="True"
                    Width="84" Height="32"
                    Style="{StaticResource SmallButtonStyle}"/>
            <Button Content="Clone" Click="Clone_Click" IsDefault="True"
                    Width="84" Margin="8,0,0,0"
                    Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
