<UserControl x:Class="GitPulse.Views.CommitGraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:GitPulse.Converters">
    <UserControl.Resources>
        <conv:HexToBrushConverter x:Key="HexToBrush"/>
        <conv:TextLengthToWidthConverter x:Key="TextLengthToWidth"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="14,7">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="HISTORY" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding Commits.Count, StringFormat='  \u00B7  {0} commits'}"
                               FontSize="11" Foreground="{StaticResource TextDimBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <Grid Grid.Column="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="11" Padding="8,3" Width="180"
                             VerticalContentAlignment="Center"/>
                    <Button Grid.Column="1" Content="Search" Command="{Binding SearchCommand}"
                            Style="{StaticResource SmallButtonStyle}" Margin="6,0,0,0"/>
                </Grid>
            </Grid>
        </Border>

        <ListBox Grid.Row="1" ItemsSource="{Binding Commits}"
                 Background="Transparent" BorderThickness="0"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="10,4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Graph lanes: fixed-width centered cells keep columns aligned
                                 even when glyphs fall back to other fonts -->
                            <ItemsControl ItemsSource="{Binding Segments}"
                                          Margin="0,0,8,0" VerticalAlignment="Center">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Text}"
                                                   Width="{Binding Text, Converter={StaticResource TextLengthToWidth}}"
                                                   TextAlignment="Center"
                                                   FontFamily="Consolas" FontSize="13"
                                                   Foreground="{Binding Color, Converter={StaticResource HexToBrush}}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock Grid.Column="1" Text="{Binding Commit.ShortSha}"
                                       FontFamily="Consolas" FontSize="11"
                                       Foreground="{StaticResource TextDimBrush}"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>

                            <TextBlock Grid.Column="2" Text="{Binding Commit.Message}"
                                       Foreground="{StaticResource TextBrush}" FontSize="13"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"/>

                            <Border Grid.Column="3" Background="{StaticResource BgInsetBrush}"
                                    CornerRadius="3" Padding="5,1" Margin="8,0,0,0"
                                    BorderBrush="{StaticResource BorderStrongBrush}" BorderThickness="1"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding BranchLabel, Converter={StaticResource NullToVisibilityConverter}}">
                                <TextBlock Text="{Binding BranchLabel}"
                                           Foreground="{Binding BranchColor, Converter={StaticResource HexToBrush}}"
                                           FontSize="10" FontWeight="SemiBold"/>
                            </Border>

                            <TextBlock Grid.Column="4" Text="{Binding Commit.Author}"
                                       Foreground="{StaticResource TextDimBrush}" FontSize="11"
                                       VerticalAlignment="Center" Margin="10,0,0,0"
                                       MaxWidth="140" TextTrimming="CharacterEllipsis"/>

                            <TextBlock Grid.Column="5" Text="{Binding Commit.TimeAgo}"
                                       Foreground="{StaticResource TextDimBrush}" FontSize="11"
                                       VerticalAlignment="Center" Margin="10,0,0,0"
                                       TextAlignment="Right" Width="70"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
